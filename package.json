{
  "name": "awda-design",
  "version": "8.0.0",
  "description": "Awda — design direction, palette system and interactive prototype for the Arabic Islamic app.",
  "type": "module",
  "private": true,
  "scripts": {
    "check": "node scripts/contrast.mjs",
    "build": "node scripts/build-single.mjs",
    "serve": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
